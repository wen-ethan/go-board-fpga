# Build artifacts — regenerated by `make` from the .v sources + .pcf
# yosys netlist / nextpnr bitstream / icepack output / icetime report
*.json
*.asc
*.bin
*.rpt

# Simulation (iverilog + GTKWave)
*.vvp
*.vcd

# macOS
.DS_Store
